---
name: us-equity-turning-point-radar
description: 构建、运行并发布美股阶段顶底概率雷达。以每周动态调整的100只常态人气股为日常池，科技优先且覆盖非科技；结合情绪修复、行业轮动、个股独立强弱、技术价带、事件与波动，生成多周期顶底路径、机会/风险双排序和临时个股分析。结果交付GitHub Pages HTML，后台保留可审计回测、校准和版本迭代。用户要求更新美股顶底雷达、查看某股阶段顶底、更新100股池、发布或验证雷达时使用。
---

# 美股阶段顶底概率雷达

版本：`1.1.1-storage-audit`。本次为原包增量升级；不得删减原有研究、回测、网页或安全功能。

## 交付契约
最终日常入口是GitHub Pages上的中文HTML，不是Markdown研究报告、Excel或只有数据的JSON。HTML必须有动态100股表、机会/风险四向箭头排序、周期切换、个股详情、临时个股输入及真实任务状态。研究逻辑不能因做页面而被删减。

这是新的长期Skill，不改写既有美股复盘、行情筛选或研究Skill。所有新增项目文件、模型、日志、缓存和中间结果保存在`D:\codex`之下。默认项目目录`D:\codex\us-equity-turning-point-radar`；工作区Skill默认置于`D:\codex\.agents\skills\us-equity-turning-point-radar`，先核对当前Codex支持的Skill搜索目录再安装，不移动其他Skill。

已知旧行情仓库名`us-share-technical-screener`，现有Skill可能名为`us-share-daily-market-html`，复盘Skill可能名为`us-market-daily-recap`。这些只是只读发现目标，必须检查本地实际内容，不能假装已经检查。不覆盖旧仓库、不重写旧首页、不把新规则写回旧Skill。新网站默认独立仓库`us-equity-turning-point-radar`。GitHub账号提示为`simondongxiao`，发布前以本地`gh auth status`与实际远端核验为准；不匹配时停止发布而非切换身份。

## 先读哪些文件
本机当前完整规范源位于`D:\codex\us-equity-turning-point-radar`；安装在`D:\codex\skills`中的Skill以该项目内同名文件和`references`为执行依据。
首次构建、模型更改和验收：完整读取`references/model-spec.md`、`references/universe-policy.md`、`references/frontend-and-runtime.md`、`references/backtest-and-iteration.md`、`references/acceptance.md`、`references/storage-memory.md`、`references/migration-v1.1.md`。
普通更新：读取项目配置、当前生产模型卡、最近成功run manifest、只读轮动接口，再按需加载上述参考，不重复从头构建项目。
股票池初始种子见`assets/universe_seed.csv`，恰好100只。它是人工选定的首版观察种子，不是已跑完当前人气统计的排名；正式上线首跑必须验证证券身份、流动性与持续人气。

## 第一次执行
1. 只读盘点本地项目、数据源、轮动快照、GitHub认证和部署配置；记录复用项及不可用项，不索要已经能在本地解析的参数。
2. 按参考规范在新项目中实现数据适配器、特征、状态、竞争风险、共同路径分布、回测校准、排序、持久化、HTML和任务网关。不能只生成SKILL.md后宣称项目完成。
3. 包内`assets/dashboard.html`是可运行的界面模板；`scripts/render_preview.py`仅用于预览，`scripts/validate_delivery.py`仅检查接口与交付约束。包内没有训练好的金融预测模型或已上线的任务网关。首次构建要补齐并实际运行这些组件；不得把预览模板冒充实盘成品。
4. 先跑单元与合成集成测试，再读取真实历史行情跑按时间的样本外检验。合成测试通过不代表策略验证成功。
5. 完成100股首次实时资格复核、真实模型推断及网页渲染；取不到足够有效数据时展示具体覆盖缺口，不填假价格、假概率或假回测。
6. 在已授权身份下新建/复用同名新仓库，部署Pages及受保护任务接口。使用真实页面URL和一次不在100股池内的有效美股，验证输入→排队→计算→结果回显。仅推送代码、仅返回200、仅查看旧缓存均不算验收完成。
7. 输出在线入口、commit、部署状态、有效数据日期、100股覆盖数、临时输入链路状态及未完成项。没有授权或服务未配置时继续完成可执行的本地工作，明确远端阻塞；禁止声称已发布。

## 日常触发
- “按这个Skill做今天更新”“更新美股顶底雷达”：确定最近完整美股交易日，更新100股及已有临时观察；读取同时间口径轮动；生成冻结预测与HTML并发布。
- “更新本周人气股池”：按周切点重选，保存新增/退出及原因，不强制每周更换。
- “分析MU的顶底”“临时看某股票”：解析证券身份，调用与日常100股相同的完整计算入口，不使用另一个缩水模型；临时股不自动挤占常态100个名额。
- “回测/校准/检查迭代”：只评价已成熟结果，训练挑战者，满足预先登记验收才晋级；不改写历史预测与稳定规则。
- “发布/验证HTML”：运行数据校验、前端测试、秘密扫描、部署并验证真实线上内容。

## 每次更新的固定顺序
证券/交易日校验 → 判断是否到每周调池时点 → 增量抓取并冻结数据 → 时间一致的轮动/情绪状态 → 个股特征与事件 → 多周期共同路径分布 → 顶底/首次触达/交易净收益 → 风险与机会独立排序 → 追加预测台账 → 结算成熟旧预测 → HTML构建与测试 → 原子发布 → 报告真实状态。
多个更新版本并存，不覆盖。盘中价格和盘后事件不得伪装为已完成日线；如仅有收盘模型，盘中请求显示最近完整交易日版本并明确说明，而非假装实时推断。

## 禁止事项
- 不把“连跌两天第三天修复”写成规律；持续时间是条件特征。
- 不把凯利公式当顶底预测器，不把阶段底部概率当交易成功概率。
- 不把VIX高、期权Put多或Gamma估计直接变成底部结论。
- 不把技术指标主观计票转换为概率；相关信息要做增量检验。
- 不在浏览器/HTML/公开GitHub/URL/localStorage内存放GitHub、数据商或模型长效密钥。
- 不让公开输入拼接shell命令、提交任意代码或触发无限付费任务。
- 不使用未来信息、事后摆动锚点、当前100只幸存股反推无偏历史业绩。
- 不用未校准0—100评分冒充百分比；不以放宽预测区间或减少交易次数冒充模型进步。
- 不把底部区域、阶段底部确认、短期反弹和买入时机混为一谈。
- 不承诺每天更准确、不强制所有股票给出可交易信号。

## 生产与挑战者口径
现行B3生产层的`p_upfirst / p_downfirst / p_unhit`是互斥首次触达分类；`p_top / p_bottom`来自同一组重加权历史路径中是否包含阶段事件的边际场景占比。后两者不是下一交易日涨跌预测，也不能在未经独立校准前称为“阶段顶/底胜率”。页面、数据契约和解释必须保留`scenario_marginal_not_independently_calibrated`状态。

影子层按非对称四态事件单独校准，顶与底可以同窗发生。它可展示条件极值带、成熟校准路径扇形图、留组回中位数敏感度和固定其他特征的指数冲击压力测试；必须标明当前100股选择偏差、非SHAP及非明日预测。影子层只有按预登记同目标回测通过后才能晋级，不能覆盖B3或改写旧预测。

## 页面与研究保留
首页只有短的市场/轮动摘要和主要表格；完整矩阵、误差归因和迭代日志放后台或折叠区。个股详情必须保留多周期价带、结构与条件路径、基本面/事件原文证据、与市场/板块不同步的原因、失效条件及尾部风险；不能简化成只有几个分数的空壳。

## 完成定义
以`references/acceptance.md`逐条输出PASS/FAIL/BLOCKED。首次没有真实训练、数据和部署时可以完成“Skill安装/界面模板”，但绝不能把它标为“100股概率雷达已上线”。日常失败须保留上一个成功版本并醒目标注旧数据时间。

## 存储与内存独立研究组（v1.1）
MU、SNDK、WDC、STX在首版名单中的主研究组统一为`storage-memory / 存储与内存`，不是新增四行。主表仍100只，首版60/40不变。原半导体/设备与算力/光通信标签保留为历史映射或多标签，不重复占名额。初始基础设施其余七只显示为“算力网络与光通信”。不得借本次迁移重置已经周更的实际股票池。

完整执行`references/storage-memory.md`：网页有一键存储入口、细分业务筛选、组内强弱和与其他科技组的轮动观察；个股因子采用按发行人剔除自身的篮子。细分样本不足降级为同行代理，不填伪造广度或共振概率。MU转强不能自动把SNDK、WDC、STX升级为底部。

共用只读轮动快照，但不修改旧复盘Skill；缺少存储细分状态时在新项目适配层补算并记录来源。价格、概率仍由同一完整引擎和校准产生，不额外主观加分。分类及特征版本变化先影子验证，未经批准不得直接套用旧校准器。

用户说“更新存储组”“看SNDK/美光/西部数据/希捷”时调用同一个完整分析入口。网页筛选只是改变可见记录，不能重算全池机会/风险分位，不能取代个股计算。更新/部署前同时执行原验收和v1.1增量验收。
