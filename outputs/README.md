# Runtime outputs

Batch snapshots, manifests and backtest reports are append-only and remain
under `D:\codex\us-equity-turning-point-radar\outputs`. Public Pages receives
only the generated `site/` build. Raw prices, ledger state and credentials are
never committed.
