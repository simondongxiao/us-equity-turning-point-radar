# 验收清单：逐条PASS / FAIL / BLOCKED

## 交付边界
- 新项目在D:\codex，旧Skill/旧行情仓库没有意外修改；检查修改清单。
- 有可调用SKILL.md与完整可执行项目，不只创建目录/空函数/TODO。
- 真实数据、合成测试、界面模板、历史验证、上线状态分别报告。

## 股票池
- 首版种子100个唯一发行人/有效代码；60/40研究覆盖桶不是伪GICS。
- 首次按近期真实数据检验资格与人气，至少300只母池或说明真实范围。
- 每周点时调池、稳定缓冲、无强制换满、科技优先且非科技>=30；历史成员保留。
- 临时个股不挤占100，失效代码/股份别名/公司行动正确处理。

## 数据与模型
- 5/10/21交易日周期与1—3日修复区分；所有时间使用同一信息截止。
- 数据读取正文、有原文时间、不反复把旧新闻当新催化。
- 市场、行业、个股可不同步；轮动接口只读并有版本。
- 摆动锚点有确认时间，特征无未来数据。
- 上先/下先/未触达和为1；同bar顺序未知不假装已知。
- 终值/期间极值/条件顶底区间不混用；共同路径概率及区间自洽。
- 固定价位触达概率跨期限单调；联合区间与边际区间分开。
- VIX/IV/OI/Gamma缺失和估计状态真实；没有无成本完美期权历史假设。
- 机会/风险/人气分开；凯利在交易分布之后，无账户金额臆测。
- 未校准、样本不足、陈旧和数据异常不能生成假概率进入正式榜。

## 回测
- 训练/校准/测试按时间，无随机跨日泄漏；跨边界标签被purge。
- 动态池历史和幸存者偏差有明确处理/限制。
- 收盘信号不能按已过收盘价成交；跳空/成本/同bar两边命中处理。
- 基准、去轮动、去期权、去Fib等比较有真实结果。
- 概率校准、价带宽度/覆盖、排序效果和净交易表现分别评价。
- 模型置信度依据真实独立样本，不用模拟路径数充当样本数。
- 旧预测不可变；多个更新版本不事后择优。
- 挑战者晋级/回滚规则已登记；合成测试不当作盈利证明。

## 前端
- 100股数量、可比较预测数量、数据时间醒目。
- 机会↓、机会↑、风险↓、风险↑均是可点击按钮且状态正确。
- 数值排序、空值永远置底、同分稳定；切换周期同步数据与排序。
- 过滤不重算全池分位；临时股映射不改变常态池排名。
- 个股有多周期价带与完整研究/条件路径/风险，不只有分数。
- 手机可操作、键盘可访问、没有XSS、没有泄露密钥。

## 单股链路与GitHub
- Pages真实可访问且为本次run_id，不是只push成功。
- 网关真实鉴权，匿名不可触发收费任务；页面无服务长效密钥。
- 池外有效股票完成解析→排队→计算→结果；缓存命中与重新计算状态明确。
- 错误代码/模糊名称/无权限/超时有真实反馈，不假成功。
- 单股和批量共享引擎；队列不丢任务、旧结果不覆盖新结果。
- runner临时磁盘之外有持久台账/模型/任务状态。
- 按周自动调整在实际部署环境验证，不能以本地未运行脚本冒充自动任务。
- 公开数据许可和私密数据隔离通过检查。

## 最终回报格式
在线URL（只有真实部署才填）；commit/run_id；交易日与数据截止；池成员数/有效预测数；校准等级；四向排序结果；池外输入验收；周更调池状态；模型回测摘要；明确的FAIL/BLOCKED及所需一次性外部配置。

## v1.1增量验收（原清单全部继续适用）
- 原包文件清单无删除；原研究章节、旧网站/旧Skill不受破坏；新增字段缺失兼容。
- 首版100个唯一symbol不变、60/40不变；四只主组为存储，剩余半导体13/基础设施7。
- 存储快捷入口显示四只；DRAM/HBM显示MU，NAND/SSD显示MU+SNDK，HDD显示WDC+STX；重叠不重复加总。
- 分类筛选/全部恢复、5/10/21周期、四箭头、空值置底、全池分数不重算、手机操作正确。
- 详情同时保留原研究块和新业务/LOO块；无真实数据时不出现编造轮动与金融数字。
- LOO按发行人排除别名/股份类别；权重和成员点时有效；未来输入拒绝；未知身份不冒充已剔除。
- 1/2同行降级proxy、0同行no_peers；缺失数据不填0，有效样本和权重覆盖可审计。
- SNDK实体与WDC分拆/复权校验有结果；没有历史分类时注明限制。
- 新存储因子有真实同窗口消融/挑战者报告，未训练标BLOCKED而非PASS。
- 已周更生产池不被种子覆盖；周更记录存储候选和退出原因；历史预测/模型不被清空。
- 真实线上存储筛选与临时SNDK分析使用同一完整引擎通过验收；未部署明确BLOCKED。
