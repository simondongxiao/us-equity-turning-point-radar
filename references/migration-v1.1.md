# v1.0 → v1.1安全增量迁移

包版本`1.1.0-storage`，Skill名称与项目/仓库名称不变。此包是完整包，不是只能覆盖几个文件的补丁。

## 未部署过v1
使用BOOTSTRAP_PROMPT.txt；完整读取新包和所有参考；创建新Skill与独立项目。不要再部署旧包后重复升级。

## 已安装/已有代码/已部署v1
先只读检查本地真实版本、git status和未提交修改。备份配置、种子、当前生产成员清单、taxonomy、缓存schema与模型卡；记录基线commit。不要用git reset --hard/clean，也不要删除模型、数据库、密钥配置或历史预测。

只合并这个新雷达Skill及项目；其他老Skill/旧网页不改。已经实现的等价或更强功能保留，不重复追加、不用模板覆盖生产首页。旧正文研究章节和业务实现仍保留。对每个变更写简短功能差异与保留项。

当本地实际池已周更，不用初始种子覆盖。对现有MU/SNDK/WDC/STX按生效时间迁移主组；不在当前池的股票不因升级被强插回去。四只在初始种子必须都在且唯一；生产池遵循原每周策略。仍在旧基础设施主组的其他当前成员按真实业务核验后归类，不盲目复制七股固定名单。

新增research_group_id、legacy_research_group、业务/行业研究标签、taxonomy版本与known/effective时间。新字段可选读取，旧结果无字段保留显示。旧预测的分类版本冻结，不更改原始历史数据；新展示映射与旧预测模型版本分开标注。

分类/UI先迁移并可回滚；模型增加存储因子单独训练挑战者，不能把旧校准器贴到新特征上。若暂未训练，保留旧概率版本并标注“本次仅分类/UI升级，存储因子未纳入”，或展示未校准状态，不夸称精度提升。

滚动发布时旧schema/新schema结果并存要隔离，缓存key含版本。异步单股若更早模型或旧分类，不覆盖新批次。原任务接口、后台持久化、安全鉴权和限额原样保留。

验证见acceptance.md。执行后输出修改文件、保留功能、迁移生效时间、测试、模型/前端分别状态、真实线上结果或BLOCKED。失败原子回滚新配置/网页，不能删掉旧成功版本。